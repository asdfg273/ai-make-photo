<br />

# **AI 绘画工作站项目文档**

***

## 一、项目概述

**项目名称**：AI 绘画工作站 v5.0

**项目定位**：基于 Stable Diffusion 的 AI 图像生成与修图工具，提供一站式 AI 绘画创作体验。

**核心目标**：为用户提供直观、高效的 AI 图像生成工具，支持文生图、图生图、局部重绘、人脸修复等多种功能。

***

## 二、功能特性

### 2.1 核心功能

| 功能模块                 | 功能描述          | 技术实现                                   |
| -------------------- | ------------- | -------------------------------------- |
| **文生图 (Text2Img)**   | 通过提示词生成图像     | StableDiffusionPipeline                |
| **图生图 (Img2Img)**    | 基于参考图生成新图像    | StableDiffusionImg2ImgPipeline         |
| **局部重绘 (Inpaint)**   | 对图像指定区域进行修复   | StableDiffusionInpaintPipeline         |
| **ControlNet**       | 姿态/边缘/深度控制生成  | ControlNetModel + OpenPose/Canny/Depth |
| **修图编辑器**            | 图像调整、滤镜、画笔、裁剪 | Pyqt6 绘图引擎                           |
| **人脸修复 (ADetailer)** | 自动人脸检测与修复     | OpenCV + 人脸检测                          |

### 2.2 扩展功能

- **多模型支持**：支持 SD 1.5 和 SDXL 模型
- **多 LoRA 挂载**：支持同时加载多个 LoRA 插件，独立权重控制
- **采样器切换**：支持 Euler A、Euler、DPM++ 2M、DDIM 等多种采样器
- **提示词模板**：内置多种风格提示词预设
- **图像管理**：自动保存、历史记录、图片预览

***

## 三、技术架构

### 3.1 架构设计

```
┌─────────────────────────────────────────────────────────────┐
│                      UI 层 (PyQt6)                          │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐   │
│  │ 主窗口      │  │ 修图编辑器  │  │ 图像预览画廊      │   │
│  └──────┬──────┘  └──────┬──────┘  └────────┬──────────┘   │
└─────────┼────────────────┼───────────────────┼──────────────┘
          │                │                   │
          ▼                ▼                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    业务逻辑层                               │
│  ┌─────────────┐  ┌─────────────┐  ┌───────────────────┐   │
│  │ Generation  │  │   Events    │  │     UI Builder    │   │
│  │   Mixin     │  │   Mixin     │  │    Mixin          │   │
│  └──────┬──────┘  └──────┬──────┘  └────────┬──────────┘   │
└─────────┼────────────────┼───────────────────┼──────────────┘
          │                │                   │
          ▼                ▼                   ▼
┌─────────────────────────────────────────────────────────────┐
│                    核心引擎层 (ModelManager)                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  StableDiffusion Pipeline + ControlNet + LoRA       │   │
│  │  - txt2img_pipe, img2img_pipe, inpaint_pipe         │   │
│  │  - controlnet_pipe, pose_detector                   │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────────────────────────────┐
│                    资源层                                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌───────────────┐   │
│  │ models  │  │ loras   │  │ photo   │  │ 配置文件        │   │
│  └─────────┘  └─────────┘  └─────────┘  └───────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 核心技术栈

| 分类        | 技术         | 版本   | 用途                  |
| --------- | ---------- | ---- | ------------------- |
| **UI 框架** | PyQt6      | 6.x  | 主界面、对话框             |
| **绘图引擎**  | PyQt6      | 6.x  | 修图编辑器               |
| **图像处理**  | PIL/Pillow | 10.x | 图像加载、处理、保存          |
| **扩散模型**  | Diffusers  | 0.x  | Stable Diffusion 管线 |
| **人脸检测**  | OpenCV     | 4.x  | ADetailer 人脸检测      |

### 3.3 显存优化策略

- **CPU Offload**：模型权重动态卸载到 CPU，节省显存
- **VAE Tiling/Slicing**：分块解码，支持大图生成
- **Attention Slicing**：注意力计算分片处理
- **xFormers**：内存高效注意力机制

***

## 四、文件结构

```
ai  make photo/
├── main.py                 # 主入口，PyQt6 主窗口
├── model_manager.py        # 模型管理器（单例）
├── config_manager.py       # 配置管理
├── translation_service.py  # 翻译服务
├── requirements.txt        # 依赖清单
├── app_config.json         # 应用配置
├── app.log                 # 运行日志
├── photo_turn/             # 修图编辑器模块
│   ├── __init__.py
│   ├── pro_editor_tk.py    # Tkinter 版修图编辑器
│   ├── pro_editor_qt.py    # Qt 版修图编辑器（备用）
│   ├── components.py       # UI 组件
│   ├── mixin_ai.py         # AI 功能混入
│   ├── mixin_filters.py    # 滤镜功能混入
│   ├── mixin_history.py    # 历史记录混入
│   └── mixin_tools.py      # 工具功能混入
├── utils/                  # 工具模块
│   ├── __init__.py
│   ├── app_events.py       # 事件处理
│   ├── app_generation.py   # 生成逻辑
│   ├── app_utils.py        # 通用工具
│   ├── image_processor.py  # 图像处理
│   ├── system_utils.py     # 系统工具
│   └── ui_builder.py       # UI 构建器
├── models/                 # 模型文件目录
│   ├── *.safetensors       # SD 模型文件
│   ├── *.ckpt              # 旧格式模型
│   └── adetailer/          # ADetailer 模型
├── loras/                  # LoRA 插件目录
│   ├── sd1.5/              # SD 1.5 专用 LoRA
│   └── sdxl/               # SDXL 专用 LoRA
├── photo/                  # 生成图像输出目录
├── logo/                   # 应用图标资源
└── venv/                   # Python 虚拟环境
```

***

## 五、安装与运行

### 5.1 环境要求

- **操作系统**：Windows 10/11 (推荐)
- **Python 版本**：3.10.x
- **GPU 要求**：NVIDIA GPU（推荐 8GB+ 显存）（没有的话cpu也可以）
- **CUDA 版本**：11.8+

### 5.2 安装步骤

```bash
# 1. 克隆或下载项目
# 2. 创建虚拟环境
python -m venv venv
venv\Scripts\activate

# 3. 安装依赖
pip install -r requirements.txt

# 4. 下载模型文件
# 将 SD 模型(.safetensors 或 .ckpt)放入 models/ 目录
# 将 LoRA 文件放入 loras/sd1.5/ 或 loras/sdxl/ 目录

# 5. 运行应用
python main.py
```

### 5.3 模型准备

| 文件类型    | 放置位置                        | 说明               |
| ------- | --------------------------- | ---------------- |
| SD 底模   | `models/*.safetensors`      | 支持 SD 1.5 和 SDXL |
| LoRA 插件 | `loras/sd1.5/*.safetensors` | SD 1.5 专用        |
| LoRA 插件 | `loras/sdxl/*.safetensors`  | SDXL 专用          |

***

## 六、使用说明

### 6.1 主界面布局

```
┌──────────────────────────────────────────────────────────────┐
│  [保存] [撤销] [重做] [重置]  │  状态栏                          │
├──────────────────────────────────────────────────────────────┤
│ 左侧控制面板                    │          画布预览区            │
│ ┌─────────────────────────┐   │                              │
│ │  AI 重绘/局部调整         │    │                              │
│ │  绘图 & 裁剪             │    │                              │
│ │  文字工具                │    │        图像预览                │
│ │  变换操作                │    │                              │
│ │  图像调整                │    │                              │
│ │  预设滤镜                │    │                              │
│ └─────────────────────────┘    │                              │
│                                │                              │
│                                └─────────────────────────────┘
└──────────────────────────────────────────────────────────────┘
```

### 6.2 快捷键

| 快捷键      | 功能     |
| -------- | ------ |
| `Ctrl+S` | 保存图像   |
| `Ctrl+Z` | 撤销操作   |
| `Ctrl+Y` | 重做操作   |
| `ESC`    | 取消当前模式 |

### 6.3 图像调整参数

| 参数  | 范围          | 说明      |
| --- | ----------- | ------- |
| 亮度  | -100 \~ 100 | 调整图像亮度  |
| 对比度 | -100 \~ 100 | 调整图像对比度 |
| 饱和度 | -100 \~ 100 | 调整色彩饱和度 |
| 锐化  | 0 \~ 100    | 图像锐化程度  |
| 色温  | -100 \~ 100 | 冷色调/暖色调 |

### 6.4 预设滤镜

- 黑白、复古、冷色调、暖色调、胶片颗粒、模糊、浮雕、边缘检测、轮廓、锐化、油画

***

## 七、核心模块详解

### 7.1 ModelManager（模型管理器）

**职责**：管理 Stable Diffusion 模型的加载、优化、推理流程

**核心方法**：

| 方法                       | 功能              |
| ------------------------ | --------------- |
| `load_model()`           | 加载指定 SD 模型      |
| `apply_multiple_loras()` | 挂载多个 LoRA 插件    |
| `prepare_controlnet()`   | 配置 ControlNet   |
| `encode_prompt()`        | 使用 Compel 编码提示词 |
| `switch_sampler()`       | 切换采样器           |
| `apply_optimizations()`  | 应用显存优化          |

**设计模式**：单例模式（SingletonMeta）

### 7.2 AIDesktopApp（主窗口）

**职责**：整合所有 UI 组件和业务逻辑

**混入类架构**：

| Mixin             | 功能       |
| ----------------- | -------- |
| `UIBuilderMixin`  | UI 构建和布局 |
| `EventMixin`      | 事件处理和信号  |
| `GenerationMixin` | 图像生成逻辑   |

### 7.3 ProImageEditor（修图编辑器）

**职责**：提供图像编辑功能

**工具功能**：

- **绘图工具**：画笔、橡皮擦、遮罩画笔
- **选择工具**：裁剪
- **文字工具**：添加、编辑文字
- **变换工具**：水平/垂直翻转、旋转
- **调整工具**：亮度、对比度、饱和度、锐化、色温
- **滤镜工具**：11 种预设滤镜

***

## 八、配置与扩展

### 8.1 配置文件 (`app_config.json`)

```json
{
  "default_model": "majicmixrealisticV6_v10.safetensors",
  "default_sampler": "Euler a",
  "output_format": "png",
  "auto_save": true,
  "show_preview": true
}
```

## 九、注意事项

1. **首次运行**：首次加载模型会自动下载必要的依赖文件，可能需要较长时间
2. **显存要求**：建议使用 8GB+ 显存的 GPU，否则可能无法加载大型模型
3. **模型文件**：模型文件较大（通常 2GB-8GB），请确保有足够的磁盘空间
4. **网络环境**：首次运行需要联网下载部分依赖，建议在网络稳定的环境下运行

