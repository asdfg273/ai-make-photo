# ============================================================
#  AI 绘画工作站 - 运行依赖（精简版,宽松钉版）
#  本机完整冻结环境见 requirements-lock.txt（pip freeze 导出）
#
#  ⚠️ PyTorch (CUDA 12.8) 请单独安装,不要直接用本文件装:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# ============================================================

# --- 深度学习核心 ---
# torch / torchvision / torchaudio 见上方说明
diffusers==0.32.2
transformers==4.49.0
accelerate>=1.0
compel>=2.0
peft>=0.19
safetensors>=0.4
bitsandbytes>=0.49 ; sys_platform == "win32"
onnxruntime-gpu

# --- 模型下载 ---
huggingface-hub>=0.26
modelscope>=1.37

# --- 图像 / 视频 ---
pillow
opencv-contrib-python    # 不要再装 opencv-python / opencv-python-headless,会互相覆盖
imageio
imageio-ffmpeg
av
controlnet_aux
ultralytics              # ADetailer 面部/手部 YOLO
scikit-image

# --- GUI ---
PyQt6>=6.7

# --- LLM 提示词增强 ---
qwen-vl-utils

# --- 翻译 ---
deep-translator
jieba

# --- 音频 / TTS ---
soundfile
sounddevice
# ChatTTS 建议源码安装: pip install git+https://github.com/2noise/ChatTTS
# GPT-SoVITS 日文/中文前端依赖（pyopenjtalk 在 Windows 需 MSVC 编译环境,装不上则英文配音降级）:
jieba-fast
cn2an
pypinyin
LangSegment
# pyopenjtalk

# --- 通用工具 ---
numpy<3
requests
tqdm
psutil
PyYAML
